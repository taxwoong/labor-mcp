# 변경 이력 (Changelog)

`labor-mcp`의 버전별 변경 내용입니다. 사용법·설치는 [README](README.md), 설계 배경과
근거 리서치는 [개발계획.md](개발계획.md)를 참고하세요.

## v1.1 — 오류 응답 계약(status) · 계산 오답 13건 수정 · 인용 검증 (2026-09-01)

자매 프로젝트 nts-tax-mcp가 v5.4에서 했던 것과 같은 방식으로, **GitHub 유사 MCP 서버
전수조사(62개 저장소·31개 문서)**와 **다중 에이전트 코드리뷰(4개 영역 병렬)**를 거쳐
확인된 실버그를 수정했습니다. 모든 항목은 실행·실호출로 재현한 뒤 고쳤고, 회귀 테스트를
함께 넣었습니다(오프라인 97건 → 144건).

### 원천 장애가 "자료 없음"으로 둔갑하던 문제 — 가장 위험했던 것

- **law.go.kr 인증 실패를 감지하지 못했습니다.** 실패 응답은 HTTP 200 + `<Response>
  <result>사용자 정보 검증에 실패하였습니다.</result>` 봉투로 오는데, 본문에 "인증"이라는
  단어가 없어 기존 가드(`"인증" and "실패"`)가 전혀 걸리지 않았습니다. 그 결과 서버 IP
  등록이 풀리거나 OC가 만료되면 **법령·판례·행정해석 3종이 조용히 `0건`을 반환**했고,
  그 오류 응답이 10분간 캐싱까지 됐습니다. 이제 봉투를 파싱해 `AUTH_ERROR`로 올리고,
  오류 응답은 캐싱하지 않습니다.
- **`status` 응답 계약을 도입했습니다** — 모든 도구가 `OK` / `NOT_FOUND`(검색 성공·0건,
  부존재로 판단 가능) / `UPSTREAM_ERROR`·`PARSE_ERROR`(원천 접근·해석 실패, **부존재 단정
  금지**) / `AUTH_ERROR` / `INVALID_INPUT` 중 하나를 반환합니다. status만으로는 LLM이
  규칙을 기억해야 하므로, 응답의 `안내` 필드에 **행동 지시를 직접** 넣었습니다(공식
  `mcp-server-fetch`가 쓰는 방식). 서버 `instructions`에도 해석 규칙을 명시했습니다.
- **노동위원회 스크래퍼에 파서 생존 판정 3단 게이트**를 넣었습니다 — ①결과 영역이 통째로
  사라짐 ②총건수는 읽히는데 목록이 0건 ③**필수 필드 공백률 50% 초과**(셀렉터 하나만 바뀌면
  건수는 정상이고 내용만 비는 부분 붕괴). 진짜 0건일 때도 카테고리 헤더는 반드시 내려오는
  것을 실측 확인했기 때문에, 추가 네트워크 왕복(카나리) 없이 구분됩니다.
- **`moel.search_both`가 타임아웃을 삼켜** 빈 배열을 반환하던 것을 고쳤습니다 — 한쪽만
  실패하면 `조회상태`로 부분 실패를 알리고, 양쪽 다 실패하면 예외로 올립니다.
- **`test_mcp_client.py`가 장애 중에도 green이었습니다** — 5단계가 `"오류" in parsed`만
  검사했는데 실제 키는 `고용노동부_행정해석_오류`였습니다. 이제 `status`와 오류 키를 함께
  보고, 검사 도구 목록도 8종 → **17종 전부**로 넓혔습니다.

### 시점 조문 조회 정확성 (nts v5.4 수정분 이식 + 노무 특유 보강)

- **같은 시행일자의 여러 공포본 중 옛 것을 고르던 문제**: 정렬 키가 시행일자 하나뿐이라,
  근로기준법 제24조를 2007-07-01 기준으로 조회하면 2006.12.21 공포본("근로조건의 명시")이
  나왔습니다. 정답은 2007.1.26 공포본("근로조건의 명시 **등**" — 서면 명시·교부 의무 신설)
  으로, **의무 도입 시점을 정반대로 답하던** 셈입니다. 정렬 키를 (시행일자, 공포일자,
  공포번호)로 확장했습니다.
- **아직 시행되지 않은 문안을 반환하던 문제**: 조문별 단계 시행 때문에 시행본 XML에는 미래
  문안까지 들어 있습니다. 이제 조문의 실제 시행일이 기준일보다 미래면 이전 시행본으로
  소급하고, 건너뛴 내역을 `주의`로 알립니다. 조문 블록의 `<조문시행일자>` 태그가 헤더의
  `<조문시행일자문자열>`과 어긋나는 사례(근기법 MST 285279 제101조)가 실측돼 **둘 다** 봅니다.
- **`as_of_date` 형식 미검증**: `"2025-03-15"`를 넣으면 오류 없이 **다른 시행본**(20241022)이
  선택됐습니다. 이제 `YYYY-MM-DD`·`YYYY.MM.DD`는 정규화하고, 그 외 형식은 `INVALID_INPUT`으로
  거부합니다. 행정해석·판례의 기간 필터도 같은 이유로 검증합니다(예전에는 필터가 통째로
  무시된 채 전 기간 결과가 나갔습니다 — 실측 181건 vs 정상 28건).
- 연혁 수집 상한을 100행·5페이지 → 500행·10페이지로 확대했습니다.
- `court="하위법원"`은 law.go.kr이 인식하지 못해 **항상 0건**이었습니다(도구 설명에는 유효한
  값으로 적혀 있었습니다). 이제 거부하고 법원명을 그대로 넣도록 안내합니다.

### 계산 오답

- **휴일근로 8시간 초과분 이중 계상** — `holiday_h=10, holiday_over8_h=2`로 호출하면
  190,000원이 나왔습니다(정답 160,000원). `holiday_h`를 **총 휴일근로시간**으로 정의했습니다.
- **주 15시간 미만 근로자에게 주휴시간을 가산**하던 문제 — 근기법 §18③은 §55·§60을 적용
  제외하고, 최저임금법 시행령 §5①은 "§55①에 따라 유급으로 처리되는 시간"만 적용기준 시간에
  넣습니다. 주 12시간 근로자의 분모가 62.6h로 부풀어 **허위 최저임금 위반**이 났습니다(52.1h).
- **`plan_type="DC형"`·"확정기여"** 가 `endswith("DC")`에 걸리지 않아 DC 가입자에게 **퇴직금
  공식 금액을 확신에 차서** 반환했습니다.
- **스키마에 없는 `성격` 값**(예: "상여금")이 기본급과 같은 최관대 버킷으로 떨어져 최저임금
  위반이 "적법"으로 뒤집혔습니다. 이제 `기타`로 강등합니다.
- **`성격="기타"`는 3축 모두 불확실**로 처리합니다 — 리소스 문서(`임금항목_산입매트릭스`)가
  그렇게 약속하는데 코드는 통상임금 축만 불확실이고 최저임금은 산입이었습니다.
- **매월 지급 성과급을 일률 불산입**하던 것을 불확실로 바꿨습니다 — 최저임금법 §6④의 불산입
  목록(시행규칙 §2)에 성과급은 없고, §6④2호는 "1개월을 초과하는 기간의 사유로 산정"하는
  것으로 한정됩니다. 없는 위반을 만들어내고 있었습니다.
- **현물 지급(§6④3호가목) 미구현** — 현물 식대가 전액 산입되어 **위반을 놓쳤습니다**.
  임금항목에 `현물: true`를 받습니다.
- **최저임금 판정을 3단계(위반|적법|불확실)로** 바꿨습니다. 불확실 항목이 있으면 최선·최악을
  모두 계산해, 어느 쪽이든 결론이 같을 때만 확정합니다.
- **월말 입사자의 1년 만료일이 하루 앞당겨지던 문제**(2024-02-29 + 12개월 → 2025-02-28)로
  연차 15일이 하루 일찍 발생했습니다. 이제 다음 달 1일로 봅니다.
- **출근율 80% 미만이면 연차 0으로 확정**하던 것을 고쳤습니다 — 근기법 §60②는 그런 해에도
  1개월 개근 시 1일을 부여하므로 법정 최소치를 밑도는 값이 나갔습니다.
- 주 소정근로시간 0·음수 시간·음수 금액 등 경계 입력을 명시적으로 거부합니다.

### 임금대장 분석 — "자료가 없는데 적법"으로 새던 경로

- **연장·야간·휴일 중 하나만 기재되면 나머지를 0시간으로 간주**하고 "적법"을 냈습니다.
  야간 30시간이 실제로 있었다면 188,285원 체불이 덮입니다. 이제 불확실로 내리고, 기재사항
  8호 검사도 세 종류를 각각 봅니다(같은 조건으로 동시에 새고 있었습니다).
- **입·퇴사 월의 일할 지급을 보정하지 않아** 3월 20일 입사자 한 명으로 "총부족액 1,256,880원
  + 상습 임금체불 제재 경고"가 떴습니다(전부 허위). 월 단위 비교의 전제가 깨지는 달은
  불확실로 분리하고 부족액 합산에서 제외합니다.
- **5인 미만 사업장의 연장 시간분 임금(§43) 미지급**을 어느 검사도 잡지 못했습니다. §56 가산
  의무는 없어도 근로 제공분 임금은 지급 대상입니다.
- 임금항목이 아예 없어도 통상임금 "적법"(통상시급 0원)이 나가던 것, §63 승인 근로자 분기에
  통상시급 가드가 빠져 있던 것을 고쳤습니다.
- **주 52시간 월 합계 추정으로는 "적법"을 확정하지 않습니다** — "4주에 13시간씩 몰아 쓴
  52시간"(주 단위 위반)을 구분할 수 없기 때문입니다.
- **상습 임금체불 제재 경고가 위반 1건·80원에도 발동**했습니다. 근기법 §43의4①의 요건
  (직전 1년 3개월분 이상 체불, 또는 5회 이상 + 3천만원 이상)에 맞춰 단계를 나눴습니다.
- **총부족액추정이 이중 계상**될 수 있음을 명시하고(최저임금을 메우면 통상시급이 올라 가산수당
  이론치도 오릅니다), 검사별 소계를 따로 제공합니다.
- **엑셀에서 온 값을 그대로 받습니다** — `None`·`"2,500,000"`·`"₩2500000"`이 예전에는
  `TypeError`/`ValueError`로 **도구 자체를 죽였습니다**. 자릿수 이상(월 임금 10억 초과 등),
  연월 형식·중복, 상시근로자수 미지정 가정도 입력 경고로 알립니다.
- **월중 퇴사자의 평균임금**은 산정 창구(§2①6호)가 월 경계와 어긋나 확정하지 않습니다.

### 응답 크기 · 계약 정리

- **`analyze_payroll` 응답이 20명×12개월에 824,854자**(50명이면 206만 자)였습니다. 위반·불확실
  건의 상세는 그대로 두고 적법 건부터 접어 **74,242자**로 줄였고, 접은 사실을 `잘림`으로
  알립니다.
- **리소스 1건이 60,750자**였고 `review_work_rules` 프롬프트가 그것을 로드하라고 지시했습니다.
  `labor_resource`에 `chapter`·`article`·`max_chars`·`start_char`를 추가했습니다(표준취업규칙
  3장만 = 2,132자).
- **MCP 리소스 URI가 퍼센트 인코딩**되어 문서에 적힌 `labor://checklist/근로계약서`로는
  `resources/read`가 성공한 적이 없었습니다. ASCII 슬러그(`labor://checklist/employment-contract`)
  로 등록하고, 목록에는 사람이 읽는 이름을 답니다. `labor_resource` 도구는 한글 키·슬러그를
  모두 받습니다.
- `moel_interpretation_search`가 `latest_first`·필터 지정 시 **최상위 키를 통째로 바꾸던**
  것(`고용노동부_행정해석` → `total`/`items`)을 통일했습니다.
- `calc_ordinary_avg_wage`만 어기던 `{결과, 계산과정, 근거, 주의사항}` 4필드 규약을 맞췄습니다.
- 계산 도구 응답의 `주의사항`에 **공인노무사 확인 고지를 코드로** 붙입니다(예전에는 서버
  instructions와 프롬프트에만 있어 도구 직접 호출 시 고지가 없었습니다).
- `mode`·`plan_type`을 입력 스키마 enum으로 노출하고, `employees` 기본값 5(=전면 적용)를
  가정할 때 그 사실을 주의사항에 명시합니다.
- 판례 본문이 고지 없이 잘리던 것에 `잘림` 안내를 붙였습니다.

### 새 도구 2종 (도구 15종 → 17종)

- **`verify_citations`** — 답변 초안에 인용한 문서번호가 실제로 존재하는지 일괄 검증합니다
  (호출당 최대 10건). 표기로 자료원을 자동 라우팅하고("2020다247190"→법원 판례,
  "근로기준정책과-3084"→고용노동부 행정해석, "07-0039"→법제처 법령해석례,
  "중앙2024부해1234"→노동위원회 판정례), **확인 / 미확인(유사 후보 제시) / 판단불가**
  3값으로 답합니다. 원천 장애는 "미확인"이 아니라 **판단불가**로 분리해 부존재 오판을 막습니다.
  문서번호 비교는 **접미 일치**로 한정했습니다 — 실무 축약이 앞쪽 기관명을 생략하는
  형태("기획재정부 근로기준정책과-73" → "근로기준정책과-73")이므로, 이 규칙 하나로
  연도만 있는 입력의 광범위 오탐과 `-73`이 `-732`에 걸리는 숫자 경계 오탐이 함께 막힙니다.
  응답에는 "실존 확인이 논거를 뒷받침한다는 뜻은 아니다"라는 한계 고지가 항상 붙습니다.
- **`check_sources_health`** — 자료원 4곳(law.go.kr 판례·법령해석례, 고용노동부 행정해석,
  노동위원회)에 알려진 검색어로 1회씩 조회해 상태와 소요시간을 보고합니다. 검색 결과가
  이상할 때 "내 서버 문제인지 원천 사이트 문제인지"를 먼저 가르는 용도입니다.

### 출처 링크 · 도구 메타데이터

- 판례·법령해석례 결과에 **원문 확인용 공개 URL**을 붙였습니다
  (`https://www.law.go.kr/precInfoP.do?precSeq=…` / `expcInfoP.do?expcSeq=…` — 둘 다 실측 확인).
  law.go.kr XML이 주는 `...상세링크`는 **`?OC=<기관코드>`가 박힌 DRF 주소**라 그대로 노출하면
  API 자격증명이 대화 컨텍스트로 새어나가므로 쓰지 않습니다. 고용노동부 행정해석은 OC 없이
  열리는 공개 뷰어가 없어 링크 대신 안건번호를 인용 키로 씁니다.
- 도구 17종 전부에 **annotations**(`readOnlyHint=true`)를 부여했습니다. 검색·검증 도구는
  `openWorldHint=true`, 계산·분석 도구는 `openWorldHint=false, idempotentHint=true`입니다.

### 보안

- **바인딩을 `0.0.0.0` → `127.0.0.1`로 바꿨습니다**(`HOST` 환경변수로 조정 가능). MCP 스펙이
  로컬 바인딩을 권고하며, `0.0.0.0`이면 SDK의 DNS 리바인딩 보호도 꺼집니다. Tailscale Funnel은
  localhost로 접속하므로 **기능 손실 없이 LAN 노출면이 사라집니다.**
- 직원 배포용 안내물(`직원안내_*.html`)과 `files.zip`에 운영 비밀 경로가 들어 있어 `.gitignore`에
  추가했습니다 — 공개 저장소에 `git add -A` 한 번이면 올라갈 위치에 있었습니다.

## v1 — 노무 특화 MCP 서버 (2026-08-28)

노무(노동법) 업무 5축 — ①근로계약서 검토·작성 ②취업규칙 검토·작성 ③노동법 문제 해결
④임금대장 분석 ⑤급여테이블 역산 설계 — 를 **도구 15종·리소스 11종·프롬프트 2종**으로
구성한 첫 버전입니다. 자매 프로젝트 [nts-mcp-server](https://github.com/taxwoong/nts-tax-mcp)의
아키텍처(Python 3.12 + FastMCP + streamable-http)를 그대로 따르고 포트만 분리했습니다(8735).

### 검색 도구 4종 — 전부 실호출 검증

- **`labor_law_article`** — 노동법령 조문·부칙·연혁을 하나의 도구에서 `mode`로 전환.
  통칭 입력을 지원하고(근기법·기간제법 등), 법령ID를 자동 필터해 동명 법령 혼입을 막습니다.
- **`moel_interpretation_search`** — **고용노동부 행정해석(질의회시)** 검색. law.go.kr의
  `moelCgmExpc` API를 쓰며, 실무 문서번호("근로기준정책과-3084" 등) 그대로 질의요지·회답
  전문을 반환합니다. 법제처 법령해석례(`expc`)를 같은 호출에서 함께 검색합니다.
  기존 `LAW_API_OC` 키로 바로 호출됐습니다 — **별도 활용신청이 필요하지 않았습니다.**
- **`labor_case_search`** — 법원 노동판례(대법원·하급심) 검색 및 본문(판시사항·판결요지·전문).
- **`nlrc_decision_search`** — **노동위원회 판정례** 스크래핑. 부당해고·부당노동행위 등
  판정례를 카테고리·판정일·관할위원회로 좁혀 판정요지까지 인라인으로 받습니다.

### 계산 도구 8종 — 결정론적, 최신 법리 반영

`check_minimum_wage`(산입범위 분해·수습 감액 3요건), `calc_ordinary_avg_wage`,
`calc_annual_leave`, `calc_weekly_holiday_pay`, `calc_overtime_pay`,
`calc_severance_pay`, `calc_dismissal_notice_pay`, `calc_wage_cut_limit`.

- 모든 계산 응답은 **`{결과, 계산과정, 근거, 주의사항}` 4필드 규약**을 따릅니다. 금액만
  던지지 않고 계산과정과 근거 조문을 함께 내보내 사용자가 검산할 수 있게 한 것입니다.
- 반영한 최신 법리: **2024-12-19 대법원 전원합의체**(재직·근무일수 조건부 정기상여의
  통상임금성), **2021다227100**(1년 기간제의 연차 최대 11일), 근로기준법 §95(감급 한도).
- 5인 미만 사업장 분기를 계산별로 분리했습니다(가산수당·연차 등 적용 자체가 달라지는 지점).
- 연도별 수치(최저임금·산입비율)는 `labor_constants.py`에 분리하고, **미등록 연도를 넣으면
  명시적 오류**가 나도록 했습니다 — 조용히 옛 수치로 계산하는 것보다 갱신을 강제하는 편이 낫습니다.

### 분석·설계 도구 2종 + 리소스 열람 1종

- **`analyze_payroll`** — 임금대장 일괄 분석. 직원×월 6종 검사(최저임금·통상임금·주52시간·
  가산수당·기재사항 등)를 돌려 **위반 | 적법 | 불확실 3단계**로 판정합니다. 자료가 없으면
  "적법"이 아니라 "불확실"로 빠지도록 설계했습니다. 입력 스키마는
  `labor_resource("schema/임금대장-입력")`.
- **`design_pay_table`** — 목표 조건에서 기본급+고정OT를 **대수적으로 역산**합니다
  (통상시급↔기본급 순환을 선형식으로 해소) → 최저임금 역검증 → 근로기준법 §17②를 충족하는
  계약서 임금조항 문안까지 생성합니다.
- **`labor_resource`** — 지식 리소스 11종을 도구로도 열람. claude.ai에서 MCP resources가
  UI에 노출되지 않는 경우에 대비한 이중 노출입니다(도구가 13종에서 15종이 된 사유).

### 지식 리소스 11종 · 프롬프트 2종

체크리스트 2(근로계약서·취업규칙), 판정표 4(임금항목 산입매트릭스·5인 미만 적용제외·
시행 중 개정법 기준선·최저임금 연도별), 입력 스키마 1(임금대장), 템플릿 4(2026 표준취업규칙
일반 98개조·단시간 96개조·직장내괴롭힘 표준안, 2025 표준근로계약서 6종).
고용노동부 표준취업규칙은 hwp 원문에서 변환해 **98개조 전수 대조(누락 0건)**했습니다.

프롬프트 `review_employment_contract`·`review_work_rules`는 체크리스트 로드 → 조문 대조 →
근거 조회 → 수치 검증 → 위반 항목별 `{조항 인용 / 근거 / 무효 시 대체 기준 / 수정 문안}`
출력까지의 순서를 강제합니다.

### v1 구현 중 확정한 사실 (실측)

- **nlrc.go.kr은 `www` 없는 호스트로 호출해야 합니다** — `www` 호스트는 POST 본문을
  유실해 검색 조건이 통째로 무시됩니다. 스크래퍼가 조용히 엉뚱한 결과를 주던 원인이었습니다.
- moelCgmExpc는 기존 law.go.kr 기관코드(OC)로 접근 가능 — 별도 신청 불필요.
- 리소스 문서의 수치와 `labor_constants.py`의 상수가 어긋나지 않는지 자동 검증합니다.

### v1에서 겪은 운영 장애와 조치

- **`run_server.bat`은 ASCII + CRLF로 저장해야 합니다.** UTF-8 한국어 주석을 넣었더니
  cmd의 CP949 파싱에서 줄바꿈이 삼켜져 `call local_env.bat`이 통째로 건너뛰어졌고,
  `LAW_API_OC`가 비어 있는 채로 서버가 떴습니다. `.gitattributes`의 `*.bat -text`로
  줄바꿈 변환을 금지하고, 경로는 전부 `%~dp0` 절대경로로 바꿨습니다(경로 없는
  `call`은 `NoDefaultCurrentDirectoryInExePath` 환경에서 실패).
- **작업 스케줄러의 기본 72시간 실행 제한을 해제**해야 상시 서버가 3일마다 죽지 않습니다
  (`setup_task_and_funnel.ps1` — ONSTART · SYSTEM 계정).
- **공개 저장소에서 운영 URL 제거**: 초기 커밋들에 Tailscale Funnel 주소가 실려 있어
  단일 커밋으로 히스토리를 재작성했고, 노출됐던 엔드포인트는 회수한 뒤 **443 아래
  비밀 경로**(`/labor-<랜덤토큰>`)로 재발급했습니다. 이 서버는 인증이 없으므로 무작위
  경로가 사실상 접근 토큰 역할을 합니다. 실제 값은 `운영정보.local.md`(.gitignore)에만 둡니다.
- Funnel 443 루트는 국세 서버가 쓰고 있어 **한 호스트에서 두 MCP 서버가 경로로 분리**되어
  동시 동작함을 실측했습니다. Tailscale이 경로 접두사를 벗겨 전달하므로 FastMCP의 `/mcp`와
  그대로 맞물립니다.

### 테스트

pytest 105건 — 오프라인 97건(계산기 골든 테스트, 임금대장 함정 케이스, nlrc 파서) +
실호출 8건. 임금대장 쪽은 격월 상여로 최저임금 미달을 은폐하는 케이스, 주 54시간 검출,
5인 미만 사업장 오검출 방지 같은 **오판을 유도하는 입력** 위주로 짰습니다.
`test_mcp_client.py`는 커넥터를 우회해 초기화→도구 목록→리소스→계산→실호출 5단계를 점검합니다.
